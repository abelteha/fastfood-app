import { Fragment } from "react";
import classes from "./Modal.module.css";
const Modal = (props) => {
  <Fragment>
    <div className={classes["back-drop"]}></div>
    <div className={classes.modal}></div>
  </Fragment>;
};
export default Modal;
